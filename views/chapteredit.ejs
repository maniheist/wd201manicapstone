<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Add Chapter Page</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <meta name="csrf-token" content="<%= csrfToken %>" />
  </head>

  <body class="bg-gray-100 flex items-center justify-center h-screen">
    <div class="absolute top-6 left-6 space-x-4">
      <a href="/show" class="p-4 text-gray-800 bg-white rounded shadow-md">
        Home<span class="ml-2 text-green-500">&#8594;</span>
      </a>
      <a href="" class="p-4 text-gray-800 bg-white rounded shadow-md">
        Create A Chapter<span class="ml-2 text-green-500">&#8594;</span>
      </a>
    </div>
    <div class="bg-white p-8 rounded shadow-md w-1/3">
      <h1 class="text-2xl text-gray-800 mb-6">Add Chapter</h1>
      <form action="/chapteredit/<%= notee.id %>" method="post">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <!-- Title Input -->
        <input
          type="text"
          name="title"
          placeholder="Enter chapter name"
          class="w-full p-3 mb-4 rounded border border-gray-300 focus:outline-none focus:ring focus:ring-blue-400"
          required
        />

        <!-- Description Textarea -->
        <div class="mt-4">
          <textarea
            name="description"
            class="w-full h-40 p-3 rounded-md border border-gray-300 focus:outline-none focus:ring focus:ring-blue-400"
            placeholder="Write something..."
            required
          ></textarea>
        </div>

        <!-- Submit Button -->
        <button
          type="submit"
          class="bg-purple-600 text-white p-3 rounded-full hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
          Submit
        </button>
      </form>
    </div>
  </body>
</html>
