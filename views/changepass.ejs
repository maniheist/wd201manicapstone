<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Change Password</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #5685e3;
      }

      .form-container {
        background-color: #ffffff;
        box-shadow: 0 4px 6px -1px rgba(174, 88, 88, 0.1),
          0 2px 4px -1px rgba(216, 100, 100, 0.06);
      }

      .form-header {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
      }

      .form-label {
        color: #4b5563;
      }

      .form-input {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid #327cdd;
        border-radius: 0.375rem;
        transition: border-color 0.2s ease-in-out;
      }

      .form-input:focus {
        border-color: #4299e1;
        outline: none;
      }

      .form-button {
        background-color: #4299e1;
        color: #ffffff;
        transition: background-color 0.2s ease-in-out;
      }

      .form-button:hover {
        background-color: #3182ce;
      }
    </style>
  </head>

  <body class="flex items-center justify-center h-screen">
    <div class="form-container p-8 rounded max-w-md w-full">
      <h2 class="form-header">Change Password</h2>
      <form action="/changepassword" method="post">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div class="mb-4">
          <label for="currentPassword" class="form-label block"
            >Current Password</label
          >
          <input
            type="password"
            id="currentPassword"
            name="currentPassword"
            required
            class="form-input"
          />
        </div>
        <div class="mb-4">
          <label for="newPassword" class="form-label block">New Password</label>
          <input
            type="password"
            id="newPassword"
            name="newPassword"
            required
            class="form-input"
          />
        </div>
        <div class="mb-6">
          <label for="confirmPassword" class="form-label block"
            >Confirm Password</label
          >
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            required
            class="form-input"
          />
        </div>
        <div class="flex items-center justify-center">
          <button type="submit" class="form-button rounded px-5 py-2">
            Change Password
          </button>
        </div>
      </form>
      <%- include("notifications.ejs") %>
    </div>
  </body>
</html>
